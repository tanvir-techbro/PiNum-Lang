#include "../include/lexer.h"
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[]) {

        // Running the loop till we hit EOF (End Of File).
        while (1) {
                // garbage code
        }

        return EXIT_SUCCESS;
}
