# AST Test 2: Nested control structures
int outer = 5;
int inner = 3;
if (outer > 0) {
        while (inner > 0) {
                if (inner == 1) {
                        print("deep\n");
                }
                inner = inner - 1;
        }
} else {
        print("else\n");
}
